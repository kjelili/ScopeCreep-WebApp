# requirements.txt
# Pinned to avoid breaking changes (notably httpx 0.28 removing `proxies=` in favor of `proxy=`).
streamlit
openai==2.11.0
httpx==0.27.2
twilio
pandas
PyMuPDF
python-docx
python-dotenv
numpy
scikit-learn