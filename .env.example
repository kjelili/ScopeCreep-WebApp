# Copy to .env and fill in. Only needed for SMS alerts.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=+440000000000

# Optional: where the SMS de-duplication ledger is stored
# (defaults to ~/.scopecreep)
# SCOPEAPP_DATA_DIR=/var/lib/scopecreep
